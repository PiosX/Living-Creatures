extends ../_includes/layout

block variables
  - var template = 'midnight'

block content
  .midnight 
    .midnight__wrapper 
      each section in midnight.data.body 
        if section.slice_label == 'border1'
          section.midnight__border 
            each item in section.items 
              p.midnight__border__text(data-animation="paragraph")=item.text
              hr.midnight__border__hr
              p.midnight__border__desc(data-animation="paragraph")=item.description

        if section.slice_type == 'highlights'
          section.midnight__highlights 
            h2.midnight__highlights__title(data-animation="title")!=section.primary.title.replace(/\n/g, '<br>')
            p.midnight__highlights__desc(data-animation="paragraph")=section.primary.description

        if section.slice_type == 'header'
          section.midnight__header
            .midnight__header__box
              p.midnight__header__text(data-animation="paragraph")=section.primary.text
              h1.midnight__header__title(data-animation="title")=section.primary.title
              p.midnight__header__subtitle(data-animation="paragraph")=section.primary.subtitle
            each item in section.items 
              p.midnight__header__subtext(data-animation="paragraph")=item.text

        if section.slice_type == 'content'
          section.midnight__content
            h2.midnight__content__title(data-animation="title")=section.primary.title
            each media in section.items 
              figure.midnight__content__media 
                img.midnight__content__media__image(data-src=media.image.url alt=media.image.alt)
                figcaption.midnight__content__media__name=media.name

        if section.slice_label == 'border2'
          section.midnight__suborder 
            each item in section.items 
              .midnight__suborder_box
                p.midnight__suborder__box__text(data-animation="paragraph")=item.text
                p.midnight__suborder__box_desc(data-animation="paragraph")=item.description
              .midnight__suborder__media 
                img.midnight__suborder__media__img(src=item.icon.url alt=item.icon.alt)

        if section.slice_type == 'informations'
          section.midnight__informations 
            h2.midnight__informations__title(data-animation="title")=section.primary.title
            .midnight__informations__box 
              each media in section.items 
                figure.midnight__informations__box__media 
                  img.midnight__informations__box__media__image(data-src=media.image.url alt=media.image.alt)
                  figcaption.midnight__informations__box__media__subtitle=media.subtitle
                  p.midnight__informations__box__desc(data-animation="paragraph")=media.description  

        if section.slice_label == 'border3'
          section.midnight__border 
            each item in section.items 
              p.midnight__border__text(data-animation="paragraph")=item.text
              hr.midnight__border__hr
              p.midnight__border__desc(data-animation="paragraph")=item.description

        if section.slice_type == 'footer'
          section.midnight__footer 
            h3.midnight__footer__title(data-animation="title")=section.primary.title
            .midnight__informations__button
              a.midnight__informations__button__link(href='/abyss')="v"